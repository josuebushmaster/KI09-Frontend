// This file used to contain an experimental split of the cart provider.
// The canonical implementation lives inside `useCart.tsx` and should be imported
// from there: `import { CartProvider } from '../hooks/useCart'`.
//
// Keep a no-op export here to avoid upstream tooling errors while we stabilize
// the module layout during development.

export {};
